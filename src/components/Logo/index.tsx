import styled from "styled-components"

import { ReactComponent as AssetLogo } from "../../assets/imgs/logo.svg"

const Logo: React.FC = () => <AssetLogo />

const StyledLogo = styled(Logo)``

export default StyledLogo
